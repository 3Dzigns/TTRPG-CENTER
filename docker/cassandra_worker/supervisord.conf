[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid

[unix_http_server]
file=/var/run/supervisor.sock
chmod=0700

[supervisorctl]
serverurl=unix:///var/run/supervisor.sock

[rpcinterface:supervisor]
supervisor.rpcinterface_factory = supervisor.rpcinterface:make_main_rpcinterface

# Cassandra Checksum Worker
[program:pass_d_checksum]
command=python3 pass_d_checksum_worker.py --jobs-dir /Transfer_Station/jobs --log-dir /Transfer_Station/Logs --poll-interval 5
directory=/opt/workers
autostart=true
autorestart=true
startretries=10
startsecs=30
stderr_logfile=/Transfer_Station/Logs/pass_d_checksum/supervisor_stderr.log
stdout_logfile=/Transfer_Station/Logs/pass_d_checksum/supervisor_stdout.log
