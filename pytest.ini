[pytest]
markers =
    integration: marks integration tests that require external services (deselect with -m "not integration")
    chaos: marks chaos verification tests (deselect with -m "not chaos")
