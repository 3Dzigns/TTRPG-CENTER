name: AI Code Review (OpenAI, Diff-Only)

on:
  workflow_dispatch: {}
  push:
    branches: ["**"]
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, edited]
    branches: ["**"]
  # This secondary trigger is needed so we can post comments on PRs from forks with a writeable token.
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review, edited]
    branches: ["**"]

# We need write perms to post PR/commit comments and to upload artifacts.
permissions:
  contents: write
  pull-requests: write
  actions: read

env:
  OPENAI_MODEL: gpt-4o-mini
  MAX_BYTES_PER_CHUNK: 120000
  FAIL_ON_SEVERITY: none  # none | high | critical
  ARTIFACT_NAME: ai-review-md

jobs:
  analyze:
    name: Analyze diff & generate review
    runs-on: ubuntu-latest
    # Only run analysis on push / pull_request / workflow_dispatch.
    if: github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Checkout repo (safe for push & PR)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine BASE/HEAD
        id: sha
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "base=${{ github.event.pull_request.base.sha }}" >> "$GITHUB_OUTPUT"
            echo "head=${{ github.event.pull_request.head.sha }}" >> "$GITHUB_OUTPUT"
          else
            BASE=$(git rev-parse HEAD~1 || true)
            if [ -z "$BASE" ]; then BASE="$(git hash-object -t tree /dev/null)"; fi
            echo "base=$BASE" >> "$GITHUB_OUTPUT"
            echo "head=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          fi

      - name: Build code-only diff
        id: diff
        shell: bash
        run: |
          set -euo pipefail
          BASE="${{ steps.sha.outputs.base }}"
          HEAD="${{ steps.sha.outputs.head }}"
          mapfile -t CODE_FILES < <(git diff --name-only "$BASE" "$HEAD" | grep -Evi '\.md$' || true)
          if [ "${#CODE_FILES[@]}" -eq 0 ]; then
            echo "has_diff=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git diff --unified=0 "$BASE" "$HEAD" -- "${CODE_FILES[@]}" > diff.patch || true
          if [ ! -s diff.patch ]; then
            echo "has_diff=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_diff=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Stop if no diff
        if: steps.diff.outputs.has_diff == 'false'
        run: echo "Nothing to review."

      - name: Ensure OPENAI_API_KEY
        if: steps.diff.outputs.has_diff == 'true'
        shell: bash
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          set -euo pipefail
          [ -n "${OPENAI_API_KEY:-}" ] || { echo "::error::OPENAI_API_KEY missing"; exit 1; }

      - name: Chunk diff
        if: steps.diff.outputs.has_diff == 'true'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .github/ai
          rm -f .github/ai/chunk_*.patch || true
          split -b "${MAX_BYTES_PER_CHUNK}" -d -a 3 diff.patch .github/ai/chunk_ || true
          shopt -s nullglob
          COUNT=0
          for f in .github/ai/chunk_*; do mv "$f" "${f}.patch"; COUNT=$((COUNT+1)); done
          if [ "$COUNT" -eq 0 ]; then cp diff.patch .github/ai/chunk_000.patch; fi

      - name: Run OpenAI review → review.md
        if: steps.diff.outputs.has_diff == 'true'
        uses: actions/github-script@v7
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_MODEL: ${{ env.OPENAI_MODEL }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');

            function fail(msg) { core.setFailed(msg); throw new Error(msg); }
            if (!process.env.OPENAI_API_KEY) fail('Missing OPENAI_API_KEY');

            // Load prompt from repo if present; otherwise inline fallback.
            let basePrompt = '';
            const promptPath = '.github/ai/prompt.txt';
            if (fs.existsSync(promptPath)) {
              basePrompt = fs.readFileSync(promptPath, 'utf8').trim();
            }
            if (!basePrompt) {
              basePrompt = [
                'You are a senior software engineer performing a peer review of a pull request.',
                '',
                'STRICT SCOPE',
                '- Review ONLY the supplied git DIFF/patch for this PR (changed lines and immediate context).',
                '- Do NOT review or critique changes made to Markdown files (*.md). Treat .md diffs as documentation/context only.',
                '- You may use .md content to understand requirements or intended behavior, but do NOT flag issues about .md changes.',
                '- Do NOT review project-wide requirements, policies, or architecture unless changed lines in code directly violate them.',
                '- Do NOT summarize the whole repo; focus only on code changes.',
                '',
                'OUTPUT FORMAT',
                '1. Summary: 2-5 bullet points.',
                '2. Findings: Numbered list. Each finding MUST include:',
                '   - File path and line(s) from the code diff',
                '   - Issue description or improvement opportunity',
                '   - Concrete suggestion with a sample code or patch snippet showing a possible resolution',
                '   - Severity: LOW | MEDIUM | HIGH | CRITICAL',
                '3. Tests: Explicitly call out missing or to-be-updated tests for the new code.',
                '4. Security/Concurrency: Only if visible in the code diff; include the lines and a sample mitigation.',
                '5. GO/NO-GO: Decide based only on the code diff.',
                '',
                'TONE',
                '- Be concise, technical, and actionable.',
                '- Always include at least one possible resolution (sample code, config change, or patch) for each issue.',
                '- If context outside the diff is needed, state clearly: Assumption: ...',
                '',
                'Remember: this is a developer code review of code changes only. Markdown files are context, not review targets.'
              ].join('\n');
            }

            const chunks = fs.readdirSync('.github/ai')
              .filter(f => f.startsWith('chunk_') && f.endsWith('.patch'))
              .sort();
            if (chunks.length === 0) fail('No diff chunks found');

            async function askOpenAI(prompt) {
              const res = await fetch('https://api.openai.com/v1/chat/completions', {
                method: 'POST',
                headers: {
                  'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`,
                  'Content-Type': 'application/json'
                },
                body: JSON.stringify({
                  model: process.env.OPENAI_MODEL || 'gpt-4o-mini',
                  temperature: 0.2,
                  messages: [
                    { role: 'system', content: 'You are a precise, terse, and helpful senior engineer.' },
                    { role: 'user', content: prompt }
                  ]
                })
              });
              const text = await res.text();
              if (!res.ok) fail(`OpenAI HTTP ${res.status}: ${text}`);
              let json;
              try { json = JSON.parse(text); } catch (e) { fail(`OpenAI JSON parse error: ${e.message}\nRaw: ${text}`); }
              return json?.choices?.[0]?.message?.content ?? '';
            }

            let parts = [];
            for (const f of chunks) {
              const diff = fs.readFileSync(path.join('.github/ai', f),'utf8');
              const prompt = `${basePrompt}\n\n--- DIFF CHUNK ${f} ---\n${diff}`;
              core.info(`Sending ${f} to OpenAI (${diff.length} bytes)`);
              const out = await askOpenAI(prompt);
              parts.push(`### Review for ${f}\n${out}`.trim());
            }
            fs.writeFileSync('.github/ai/review.md', parts.join('\n\n---\n\n'));
            core.info('Wrote .github/ai/review.md');

      - name: Upload review artifact
        if: steps.diff.outputs.has_diff == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: .github/ai/review.md

      # For non-PR runs (push/dispatch), post a commit comment now.
      - name: Post commit comment (non-PR)
        if: steps.diff.outputs.has_diff == 'true' && github.event_name != 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.github/ai/review.md', 'utf8').trim();
            if (!body) { core.setFailed('review.md empty'); }
            else {
              await github.rest.repos.createCommitComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                commit_sha: context.sha,
                body
              });
            }

  comment:
    name: Post review on PR (fork-safe)
    runs-on: ubuntu-latest
    needs: analyze
    # Only run this on pull_request_target so we have a writeable token on the base repo.
    if: github.event_name == 'pull_request_target'
    steps:
      - name: Download review artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}

      - name: Post PR review comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('review.md','utf8').trim();
            if (!body) { core.setFailed('review.md empty'); }
            else {
              // On pull_request_target we can safely access pull_request.number.
              await github.rest.pulls.createReview({
                owner: context.repo.owner,
                repo: context.repo.repo,
                pull_number: context.payload.pull_request.number,
                event: 'COMMENT',
                body
              });
            }
