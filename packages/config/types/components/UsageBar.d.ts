export interface UsageBarProps {
    used: number;
    total: number;
    label?: string;
    className?: string;
}
export declare function UsageBar({ used, total, label, className }: UsageBarProps): any;
